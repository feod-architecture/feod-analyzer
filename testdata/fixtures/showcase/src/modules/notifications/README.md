# Notifications Module

Displays user-facing notification state in the app shell.

| Boundary | Details |
| --- | --- |
| Public API | `NotificationBell` |
| Consumers | app shell, profile |

Notification UI can depend on auth context, but other modules should not import nested files.

