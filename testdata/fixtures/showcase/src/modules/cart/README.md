# Cart Module

Stores cart state and exposes checkout-facing cart UI.

| Boundary | Details |
| --- | --- |
| Public API | cart model, `CartSummary` |
| Consumers | checkout, catalog, app shell |

Cart domain rules live in `model/cart`.

