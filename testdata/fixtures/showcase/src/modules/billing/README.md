# Billing Module

Renders account billing controls and subscription status.

| Boundary | Details |
| --- | --- |
| Public API | `BillingPanel` |
| Depends on | profile and auth context |

Billing screens should import from the module public API instead of nested UI files.

